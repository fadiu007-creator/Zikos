'use client'
import {FormEvent,useState} from 'react'
import {getSupabase} from '@/lib/supabase'

export default function AuthPage(){
 const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [signup,setSignup]=useState(false); const [busy,setBusy]=useState(false); const [message,setMessage]=useState('')
 async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMessage('');const supabase=getSupabase();if(!supabase){setMessage('Supabase is not configured.');setBusy(false);return}const result=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});if(result.error)setMessage(result.error.message);else setMessage(signup?'Check your email to confirm your account.':'Signed in successfully.');setBusy(false)}
 return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',padding:24,background:'var(--soft)'}}><div className="card" style={{width:'100%',maxWidth:430,padding:28}}><a href="/" style={{fontWeight:900,fontSize:25,color:'var(--brand)',textDecoration:'none'}}>zikos.</a><h1 style={{margin:'26px 0 8px'}}>{signup?'Create your account':'Welcome back'}</h1><p style={{color:'var(--muted)',marginTop:0}}>{signup?'Create an account to save homes and list property.':'Sign in to your Zikos account.'}</p><form onSubmit={submit} style={{display:'grid',gap:14}}><input required type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} style={{padding:14,border:'1px solid #d9e3df',borderRadius:10}}/><input required minLength={6} type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} style={{padding:14,border:'1px solid #d9e3df',borderRadius:10}}/><button disabled={busy} style={{padding:14,border:0,borderRadius:10,background:'var(--brand)',color:'#fff',fontWeight:800}}>{busy?'Please wait…':signup?'Create account':'Sign in'}</button></form>{message&&<p style={{marginTop:16,color:'var(--muted)'}}>{message}</p>}<button onClick={()=>setSignup(!signup)} style={{marginTop:12,border:0,background:'none',color:'var(--brand)',fontWeight:700,cursor:'pointer'}}>{signup?'Already have an account? Sign in':'New to Zikos? Create an account'}</button></div></main>}
